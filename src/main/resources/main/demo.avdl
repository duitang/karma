@namespace("com.duitang.service.demo")
protocol DemoService {
	union{null, string} memory_getString(string key);
	boolean memory_setString(string key, string value, int ttl);
    union{null, bytes} memory_getBytes(string key);
    boolean memory_setBytes(string key, bytes value, int ttl);
    string trace_msg(string key, long ttl);
}